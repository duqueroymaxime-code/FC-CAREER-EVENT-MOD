export default function ImportGuidePage() {
  const card = {
    padding: 24,
    borderRadius: 24,
    border: "1px solid rgba(255,255,255,.12)",
    background:
      "linear-gradient(180deg, rgba(15,23,42,.86), rgba(15,23,42,.62))",
    boxShadow: "0 24px 80px rgba(0,0,0,.28)",
  };

  const link = {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "12px 16px",
    borderRadius: 16,
    background: "#bef264",
    color: "#020617",
    fontWeight: 900,
    textDecoration: "none",
    marginRight: 10,
    marginTop: 10,
  };

  return (
    <main
      style={{
        minHeight: "100vh",
        padding: 32,
        background:
          "radial-gradient(circle at 15% 10%, rgba(190,242,100,.22), transparent 28%), radial-gradient(circle at 85% 12%, rgba(34,211,238,.22), transparent 28%), #020617",
        color: "white",
        fontFamily: "Arial, sans-serif",
      }}
    >
      <div style={{ maxWidth: 1080, margin: "0 auto" }}>
        <p
          style={{
            color: "#22d3ee",
            fontWeight: 900,
            textTransform: "uppercase",
            letterSpacing: 2,
          }}
        >
          FC Career Hub
        </p>

        <h1 style={{ fontSize: 48, marginBottom: 12 }}>
          Importer sa carrière FC26
        </h1>

        <p style={{ color: "#cbd5e1", fontSize: 18, maxWidth: 780 }}>
          Méthode recommandée : remplis quatre fichiers simples, lance le
          convertisseur, puis colle le JSON généré dans FC Career Hub.
        </p>

        <section style={{ display: "grid", gap: 18, marginTop: 30 }}>
          <div style={card}>
            <h2>Vue d’ensemble</h2>
            <p style={{ color: "#cbd5e1" }}>
              Tu n’as pas besoin d’écrire un gros JSON à la main. Tu remplis
              seulement quatre fichiers lisibles :
            </p>

            <ul>
              <li>
                <b>club.json</b> — club, ligue, budget, saison, coach
              </li>
              <li>
                <b>squad.csv</b> — effectif
              </li>
              <li>
                <b>fixtures.csv</b> — matchs et résultats
              </li>
              <li>
                <b>table.csv</b> — classement
              </li>
            </ul>
          </div>

          <div style={card}>
            <h2>Étape 1 — Récupérer les infos dans FC26</h2>
            <ol>
              <li>Ouvre EA App en mode hors ligne.</li>
              <li>Lance FC26.</li>
              <li>Charge ta carrière.</li>
              <li>Ouvre FC 26 Live Editor.</li>
              <li>
                Récupère ton club, ton budget, ton effectif, tes résultats et
                ton classement.
              </li>
            </ol>
          </div>

          <div style={card}>
            <h2>Étape 2 — Remplir les fichiers</h2>
            <p style={{ color: "#cbd5e1" }}>
              Va dans le dossier <b>public/fc26-import-pack</b> et complète les
              fichiers.
            </p>

            <a style={link} href="/fc26-import-pack/club.json">
              Voir club.json
            </a>
            <a style={link} href="/fc26-import-pack/squad.csv">
              Voir squad.csv
            </a>
            <a style={link} href="/fc26-import-pack/fixtures.csv">
              Voir fixtures.csv
            </a>
            <a style={link} href="/fc26-import-pack/table.csv">
              Voir table.csv
            </a>
            <a style={link} href="/fc26-import-pack/MODE-D-EMPLOI.txt">
              Mode d’emploi
            </a>
          </div>

          <div style={card}>
            <h2>Étape 3 — Convertir en JSON</h2>
            <p style={{ color: "#cbd5e1" }}>
              Dans le terminal, à la racine du projet, lance :
            </p>

            <pre
              style={{
                padding: 18,
                borderRadius: 18,
                background: "rgba(0,0,0,.38)",
                overflowX: "auto",
              }}
            >
              {"node tools\\convert-fc26-import-pack.cjs"}
            </pre>

            <p style={{ color: "#cbd5e1" }}>
              Le fichier généré sera :
              <br />
              <b>public/imports/fc26-career-import.json</b>
            </p>
          </div>

          <div style={card}>
            <h2>Étape 4 — Importer dans FC Career Hub</h2>
            <ol>
              <li>
                Ouvre <b>public/imports/fc26-career-import.json</b>.
              </li>
              <li>Copie tout le contenu du fichier.</li>
              <li>Va dans FC Career Hub.</li>
              <li>Clique sur <b>Manager Career</b>.</li>
              <li>Choisis <b>Import FC26</b>.</li>
              <li>Colle le JSON.</li>
              <li>
                Clique sur <b>Créer depuis données FC26</b>.
              </li>
            </ol>
          </div>

          <div style={card}>
            <h2>Résumé ultra court</h2>

            <pre
              style={{
                padding: 18,
                borderRadius: 18,
                background: "rgba(0,0,0,.38)",
                overflowX: "auto",
                whiteSpace: "pre-wrap",
              }}
            >
              {
                "1. Remplir public/fc26-import-pack\n" +
                "2. Lancer node tools\\convert-fc26-import-pack.cjs\n" +
                "3. Copier public/imports/fc26-career-import.json\n" +
                "4. Coller dans Manager Career > Import FC26"
              }
            </pre>
          </div>
        </section>
      </div>
    </main>
  );
}
